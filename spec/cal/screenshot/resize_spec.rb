require "spec_helper"

describe Cal::Screenshot::Resize do
  it "has a version number" do
    expect(Cal::Screenshot::Resize::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
